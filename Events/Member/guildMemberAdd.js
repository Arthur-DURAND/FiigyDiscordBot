const { Client } = require("discord.js")
const RoleUtil = require("../../Utils/RoleUtil")

module.exports = {
	name: "guildMemberAdd",

	execute(member) {
		// console.log("guildMemberAdd")

		//RoleUtil.giveRole(member.guild, member, "🌟┃Arrivant")
	}
}